{
  "name": "@acme/auth",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./middleware": "./src/middleware.ts",
    "./client": "./src/client.ts",
    "./env": "./env.ts"
  },
  "license": "MIT",
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "format": "biome format .",
    "lint": "biome lint .",
    "check": "biome check .",
    "generate": "dotenv -e ../../.env -- bunx @better-auth/cli generate --config script/auth-cli.ts --output ../db/src/auth-schema.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@acme/config": "workspace:*",
    "@acme/db": "workspace:*",
    "better-auth": "1.4.6",
    "zod": "4.1.13"
  },
  "devDependencies": {
    "@acme/tsconfig": "workspace:*",
    "@better-auth/cli": "1.4.6",
    "typescript": "5.9.3"
  }
}
